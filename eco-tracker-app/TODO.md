# TODO

- [ ] Change `React.FC` usage to `function` component syntax in `src/components/WeatherWidget.tsx`.
- [ ] Ensure TypeScript props typing remains correct and the component still exports as default.
- [ ] Run typecheck/build (if available) to confirm no TS errors.

